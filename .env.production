OPENAI_API_KEY=sk-prod-REPLACE_ME
NEXT_PUBLIC_CHATKIT_WORKFLOW_ID=wf_prod_REPLACE_ME

# Notes:
# - Replace the placeholders above with your real production API key and workflow id.
# - Do NOT commit real secrets to the repository. Use your deployment platform's secret store
#   (Vercel/Netlify/Azure/GCP) or an encrypted secrets manager in CI.